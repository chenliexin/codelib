transform
    support
        -webkit-, -ms-
    translate 移动
        translate(x[, y])
        translate3d(x, y, z)
        translateX(x)
        translateY(y)
        translateZ(z)
    rotate 旋转
        rotate(deg)
        rotate3d(x, y, z, deg)
        rotateX(deg)
        rotateY(deg)
        rotateX(deg)
    scale 缩放
        scale(x[, y])
        scale3d(x,y,z)
        scaleX(x)
        scaleY(y)
        scaleZ(z)
    skew 扭曲
        skew(x-deg[, y-deg])
        skewX(deg)
        skewY(deg)
    perspective 透视
        perspective-origin
    transform-style
        flat 
        preserve-3d
    transform-origin
    backface-visibility
        visible | hidden

    matrix

transition 